{
  "name": "01-npm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}

/*npm init会生成package.json文件,package.json 位于模块的目录下,
用于定义包的属性,记录包的信息，锁定大版本*/

//参数--yes表明不出现提问设置,所有参数采用默认值 

//在测试和学习阶段，用 npm init --yes 就可以了